# Bee AI Agent Frontend

Vue3 + Axios 前端项目，包含应用主页、AI 恋爱大师聊天室、AI 超级智能体聊天室。

## 启动

```bash
npm install
npm run dev
```

默认后端接口前缀为：

```text
http://localhost:8123/api
```

如需调整，可修改 `.env.development` 中的 `VITE_API_BASE_URL`。
